import { SubjectListView } from 'src/sections/subject/view';

// ----------------------------------------------------------------------

export const metadata = { title: `Subject list` };

export default function Page() {
  return <SubjectListView />;
}